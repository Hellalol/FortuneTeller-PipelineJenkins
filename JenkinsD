pipeline {

     agent { docker
     stages {
        stage ('Deploy') {
            steps {
                echo 'Deploying...'
                sh 'mvn spring-boot:run'
            }
        }
     }
}
}